[package]
name = "riverlub-whatsapp-brain-desktop"
version = "0.1.0"
description = "RiverLub WhatsApp Brain desktop shell"
authors = ["RiverLub"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "1.5", features = [] }

[dependencies]
tauri = { version = "1.6", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
